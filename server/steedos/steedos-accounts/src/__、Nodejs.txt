



core 工具相关；
  ! 获取 getMergedTenant && getSettings && getTenant 的返回结果 
  !能 密码登陆 
  !能 手机号密码登陆 
  !能 邮箱密码登陆  
  !判断是否可以发送邮箱 

database-mongo
  ? 构造 Mongo || 继承属性constructor() {}  ||  封装增删改查语句 比如createUser、findUserByMobile、updateSession、removeEmail

i18n
  ? 中英文 JSON 文件

password
  ? types 指定类型
  ? utils 工具类处理
  ? accounts-password.ts 主逻辑处理 && 密码相关的逻辑处理

rest-express
  ? express-middleware.ts； express.Router() 请求地址
  ? user-loader.ts 
  /endpoints
  /utils


saml-idp
  ? 单点登陆


db.ts
index.ts
mail.ts